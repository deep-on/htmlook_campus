# HyperFrames Composition Project — teacher-record sample

```bash
npx hyperframes preview
npx hyperframes render
npx hyperframes lint
```

- `index.html` — 15s 학생 관찰 기록부 teaser
- `meta.json` — project metadata
