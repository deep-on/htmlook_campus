# HyperFrames Composition Project — teacher-quiz sample (15s teaser)

```bash
npx hyperframes preview
npx hyperframes render
npx hyperframes lint
```

- `index.html` — 단원평가 paper teaser
- `meta.json` — project metadata
