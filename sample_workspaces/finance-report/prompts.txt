LLM prompts for the finance-report scenario — English ×5.
Drop any of these into your LLM (Claude, Codex, Llama via Ollama,
Gemini, GPT, etc.) with the HTMLook MCP attached. The two named
moves — cite a cell, narrate the variance into the report — are
[1] and [3].

Copy each prompt verbatim. The LLM is reading Atlas Manufacturing's
q3_model.xlsx on the left pane and writing into report.html
(the Q3 2026 CFO Brief draft) on the right.

────────────────────────────────────────────────────────────────────
[1] Cite the EBITDA variance cell (the main move)
────────────────────────────────────────────────────────────────────
You are looking at q3_model.xlsx on the left pane and report.html
(Q3 2026 CFO Brief draft) on the right. Use htmlook_cite to read
cell EBITDA!B14 (Q3 actual) and pair it with EBITDA!C14 (Q3 plan)
and EBITDA!D14 (delta vs plan). Quote the three values exactly as
they appear in the model and call out whether the delta is positive
or negative. Do not write the narrative yet — just the cell-cite
trio.

────────────────────────────────────────────────────────────────────
[2] Narrate the variance
────────────────────────────────────────────────────────────────────
Now narrate the EBITDA variance from prompt [1] in one sentence
suitable for a CFO brief: lead with the dollar delta, attribute the
driver to a specific row in COGS or OpEx (cite that cell too), and
keep the voice board-ready (no adjectives, active verbs only). Hold
the narrative — do not write it into the report yet.

────────────────────────────────────────────────────────────────────
[3] Apply edit — insert the paragraph (the second main move)
────────────────────────────────────────────────────────────────────
Apply the narrative from prompt [2] into report.html. Insert it as
a new paragraph under the section heading "Q3 EBITDA · variance vs
plan". Cross-link the cell refs in the paragraph as inline
monospace tags (e.g. <code>EBITDA!B14</code>) so the audit trail
back to the model is visible in the rendered report. Return the
edit as a unified diff so I can htmlook_apply_edit it directly.

────────────────────────────────────────────────────────────────────
[4] Sweep cross-tab — Revenue and Working Capital
────────────────────────────────────────────────────────────────────
Repeat the cell-cite + narrative loop for two more rows:
(a) Revenue!B12 vs Revenue!C12 (Q3 actual vs plan), and
(b) WC!B8 vs WC!C8 (Q3 working capital vs plan). For each, cite
the cell, narrate in one sentence, and apply the edit to the
matching section in report.html. Return both as a single
combined diff.

────────────────────────────────────────────────────────────────────
[5] Draft the recommendation block
────────────────────────────────────────────────────────────────────
Read the three narrative paragraphs you just inserted into
report.html. Draft a three-bullet recommendation block for the
"Looking ahead" section: one bullet per variance, each with the
proposed Q4 action (e.g. "renegotiate Castings input contract by
30 Sep") and an owner (e.g. "Plant ops"). Apply edit. Keep the
existing voice — terse, future-tense, no marketing.
