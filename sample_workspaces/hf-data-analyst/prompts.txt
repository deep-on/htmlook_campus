LLM prompts for the data-analyst scenario — English ×5.
Drop any of these into your LLM (Claude, Codex, Llama via Ollama,
Gemini, GPT, etc.) with the HTMLook MCP attached. The two named
moves — iterate the query, weight by ACV — are [2] and [4].

Copy each prompt verbatim. The LLM is iterating an April MRR query
in the fictional Pulse Analytics workspace.

────────────────────────────────────────────────────────────────────
[1] Cite the SELECT column
────────────────────────────────────────────────────────────────────
You are looking at query.sql on the left pane and the rendered chart
on the right. Use htmlook_cite("left", "L7-L12") to read the SELECT
clause. Quote the exact column list back to me, and tell me which
column is driving the bar grouping in the right-pane chart. Do not
patch anything yet — just the citation.

────────────────────────────────────────────────────────────────────
[2] Add a tier breakdown (the main move)
────────────────────────────────────────────────────────────────────
Iterate the query. Add a per-tier breakdown to the SELECT and the
GROUP BY so the right-pane chart splits MRR by tier (free, pro,
enterprise). Return it as a unified diff against query.sql so I
can htmlook_apply_edit it directly. After the apply, the right
pane should redraw with three series.

────────────────────────────────────────────────────────────────────
[3] Annotate the chart
────────────────────────────────────────────────────────────────────
The chart now shows MRR by tier for the last six months. Cite the
April enterprise bar (htmlook_cite right "bar:apr:enterprise") and
write a one-line annotation that calls out the +18% MoM swing. Keep
the wording board-deck-quiet — no exclamation, no emoji, past tense.

────────────────────────────────────────────────────────────────────
[4] Switch to a weighted average by ACV (the second main move)
────────────────────────────────────────────────────────────────────
The April spike has one outlier customer dominating the enterprise
bar. Patch the query to compute a weighted average MRR by ACV
instead of a flat sum, so the chart redraws with the outlier
properly diluted. Return as a unified diff. Cite the row you
weighted around with htmlook_cite right "row:cust_4471".

────────────────────────────────────────────────────────────────────
[5] Narrate the insight
────────────────────────────────────────────────────────────────────
Now write a 4-sentence narrative summary of what the chart shows
after the weighted-average patch: the headline number, the tier
that drove it, the supporting metrics (NRR, logo churn, ACV), and
the one caveat. Tone: quiet, factual, board-deck. This goes under
the chart in the April readout deck.
