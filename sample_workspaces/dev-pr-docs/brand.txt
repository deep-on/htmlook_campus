WorkflowKit — fictional open-source library for the dev-pr-docs scenario.

Tagline:        Code + docs, one view
URL:            github.com/workflowkit/workflowkit
PR shown:       #847 — feat(api): rename listJobs -> queryJobs + paging cursor
One-liner:      Merged to main.

Palette
  --bg          #0a0a0a   pure black, terminal background
  --green       #22c55e   primary brand (GitHub merged-green)
  --cyan        #67e8f9   secondary accent (terminal cursor)
  --soft        rgba(225, 235, 230, 0.55)   muted text on dark
  --ink         #ffffff   pure text
  --diff-add    rgba(34, 197, 94, 0.18)     green wash for + lines
  --diff-del    rgba(248, 113, 113, 0.18)   red wash for - lines

Typography
  Display       Inter / Pretendard, 700-800 weight, tight tracking
  Mono (URL,
   diff, code)  SF Mono / Menlo / ui-monospace, 2-4px tracking

Motion language
  Terminal cursor blink. Stat counters tick up by digit. Kinetic
  type lands one chunk at a time, never letter-stagger (this is a
  developer surface, not a luxury ad). Faux git graph: branch
  curve draws in, then commit dots pop, then merge node + label
  fade in last. No background drift — keep the canvas still and
  technical.

Copy guardrails
  Three stat lines:    "3 files changed" / "PR #847" / "+128 / -42"
  Outro mark:          "merged to main" wordmark + repo URL
  Voice:               quiet, terminal-y, factual. No exclamation
                       marks. No emoji. No marketing fluff.
  Avoid:               "ship", "blazing", "magic", "AI-powered" —
                       this is for a working dev, not a launch.
